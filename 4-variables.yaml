- name: variables using in ansible playbook
  hosts: localhost
  tasks:
  - name: check variables
    ansible.builtin.debug:
      msg: "Hello Rajaa, I am learning every course from today till 100 dayss"
